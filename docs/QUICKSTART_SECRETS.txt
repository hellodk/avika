┌─────────────────────────────────────────────────────────────────┐
│                  GITHUB SECRETS SETUP - QUICK START             │
└─────────────────────────────────────────────────────────────────┘

📋 STEP 1: CREATE DOCKER HUB ACCESS TOKEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to: https://hub.docker.com
2. Login → Click your username (top right) → Account Settings
3. Click "Security" (left sidebar)
4. Scroll to "Access Tokens" → Click "New Access Token"
5. Description: "GitHub Actions - NGINX Manager"
6. Permissions: "Read, Write, Delete"
7. Click "Generate"
8. ⚠️  COPY THE TOKEN NOW (you won't see it again!)

   Example: dckr_pat_1234567890abcdefghijklmnopqrstuvwxyz


📋 STEP 2: ADD SECRETS TO GITHUB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to your GitHub repository
2. Click: Settings → Secrets and variables → Actions
3. Click "New repository secret"

   Secret #1:
   ┌─────────────────────────────────────┐
   │ Name:   DOCKER_USERNAME             │
   │ Secret: your-dockerhub-username     │
   └─────────────────────────────────────┘

4. Click "Add secret"
5. Click "New repository secret" again

   Secret #2:
   ┌─────────────────────────────────────┐
   │ Name:   DOCKER_PASSWORD             │
   │ Secret: dckr_pat_abc123...          │
   │         (paste the token from Step 1)│
   └─────────────────────────────────────┘

6. Click "Add secret"


📋 STEP 3: TEST THE SETUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Option A - Automatic (Recommended):
───────────────────────────────────
cd /home/dk/Documents/git/nginx-manager
git add .
git commit -m "feat: enable CI/CD pipeline"
git push origin main

→ Go to GitHub → Actions tab → Watch the build!


Option B - Manual Trigger:
──────────────────────────
1. Go to GitHub → Actions tab
2. Click "Build and Push Agent" (left sidebar)
3. Click "Run workflow" button
4. Select branch: main, bump: patch
5. Click "Run workflow"


✅ VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After setup, you should see:

GitHub (Settings → Secrets → Actions):
  ✓ DOCKER_USERNAME  ••••••••
  ✓ DOCKER_PASSWORD  ••••••••

After first build (Actions tab):
  ✓ Workflow completes successfully
  ✓ New release created
  ✓ Image pushed to Docker Hub


🔍 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Problem: "Settings tab not visible"
→ You need admin access to the repository

Problem: "Authentication failed"
→ Check secret names are EXACTLY: DOCKER_USERNAME and DOCKER_PASSWORD
→ Verify token hasn't expired
→ Ensure you copied the complete token

Problem: "Permission denied"
→ Token needs "Write" permissions
→ Regenerate token with correct permissions


📚 MORE INFO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Detailed guide:  docs/GITHUB_SECRETS_SETUP.md
Versioning info: docs/VERSIONING_GUIDE.md
Docker config:   docs/DOCKER_CONFIG.md


┌─────────────────────────────────────────────────────────────────┐
│  🎉 Once set up, every push to main will automatically build    │
│     and push a new versioned Docker image!                      │
└─────────────────────────────────────────────────────────────────┘
