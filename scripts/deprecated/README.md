# Deprecated Scripts

These scripts have been superseded by the unified `avk` CLI (`scripts/avk`).

| Script | Replaced By |
|--------|------------|
| `run_all_tests.sh` | `avk test all` |
| `load_test_50k.sh` | `avk test load` (interactive, select "Quick") |
| `load_test_50k_100agents.sh` | `avk test load` (interactive, select "Full") |
| `build_images.sh` | `avk build docker` |
| `docker-build.sh` | `avk build docker` |
| `onboard-issues-to-project.sh` | One-time utility, no longer needed |
| `extract_all_agent_instructions.py` | One-time utility, no longer needed |
| `et` | Leftover temp file |
