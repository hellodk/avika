server:
  grpc_port: 5020
  http_port: 5021
  metrics_port: 5022

database:
  dsn: "postgres://admin:CHANGE_ME_SECURE_PASSWORD@localhost:5432/nginx_manager?sslmode=disable"
  max_open_conns: 25

clickhouse:
  address: "localhost:9000"
  database: "nginx_analytics"

kafka:
  brokers: "localhost:29092"

